# ── Render deployment requirements ──────────────────────────
# Pinned to stable versions compatible with Python 3.11

flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0

# Use pre-built wheels — avoid source compilation
numpy==1.26.4
pandas==2.2.2
yfinance==0.2.40
requests==2.32.3
ta==0.11.0

# Utilities
python-dotenv==1.0.1
loguru==0.7.2